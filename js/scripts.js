/**
 * @file
 * Behaviors for the BioData.pt theme.
 */

(function ($, Drupal) {
  Drupal.behaviors.biodataptBS5 = {
    attach() {
      // BioData.pt JavaScript behaviors goes here.
    },
  };
})(window.jQuery, window.Drupal);
